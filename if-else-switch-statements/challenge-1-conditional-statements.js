// Conditional Statements - Challenge 1 (if/else) Password

let password = "fds_923";

if (password.length < 8) {
    console.log("Your password is too short");
}
else {
    console.log(`${password}`);
}
